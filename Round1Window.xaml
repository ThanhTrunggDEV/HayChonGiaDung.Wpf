<Window x:Class="HayChonGiaDung.Wpf.Round1Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vòng 1 - Bàn Tay Vàng"
        WindowState="Maximized"
        Background="{StaticResource Surface}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel>
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                <TextBlock Text="Vòng 1: Bàn Tay Vàng" FontSize="30" FontWeight="Bold"/>
                <TextBlock x:Name="RoundProgText" Text="Câu 1/4" Opacity="0.85" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                <TextBlock Text="Đúng:"/>
                <TextBlock x:Name="CorrectCount" FontWeight="Bold"/>
            </StackPanel>
        </DockPanel>

        <!-- Main card -->
        <Border Grid.Row="1" Background="{StaticResource Card}" CornerRadius="18" Padding="20" Margin="0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: product info -->
                <StackPanel Grid.Column="0" Margin="0,0,18,0">
                    <TextBlock x:Name="ProductName" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <Border Background="#1D2748" CornerRadius="10" Padding="8" Margin="0,8,0,12">
                        <TextBlock x:Name="QuantityText" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    </Border>
                    <TextBlock Text="Mô tả" FontSize="22" FontWeight="Bold" Margin="0,4,0,6"/>
                    <ScrollViewer Height="140" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ProductDesc" TextWrapping="Wrap" Opacity="0.9" FontSize="18"/>
                    </ScrollViewer>

                    <Border CornerRadius="12" Background="#121C3C" Padding="10" Margin="0,12,0,0">
                        <Image x:Name="ProductImage" Height="280" Stretch="Uniform"/>
                    </Border>
                </StackPanel>

                <!-- RIGHT: question & actions -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="520">
                    <TextBlock Text="GIÁ ẨN" FontSize="18" Opacity="0.8" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="Question" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="0,4,0,16"/>

                    <UniformGrid x:Name="HigherLowerPanel" Columns="2" Rows="1" Margin="0,6,0,6">
                        <Button x:Name="HigherButton" Content="CAO HƠN" Height="70" FontSize="22" Click="Higher_Click" Margin="6"/>
                        <Button x:Name="LowerButton" Content="THẤP HƠN" Height="70" FontSize="22" Click="Lower_Click" Margin="6"/>
                    </UniformGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="SwapButton" Content="Đổi sản phẩm" Click="Swap_Click"/>
                    </StackPanel>

                    <TextBlock x:Name="Feedback" FontSize="18" TextAlignment="Center" Margin="0,12,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Kết thúc vòng" Click="Finish_Click"/>
        </StackPanel>
    </Grid>
</Window>
