<Window x:Class="HayChonGiaDung.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hãy Chọn Giá Đúng" Height="720" Width="1100" Background="{StaticResource Surface}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Hãy Chọn Giá Đúng" FontSize="30" FontWeight="Bold"/>
    <Border Grid.Row="1" Background="{StaticResource Card}" CornerRadius="16" Padding="20" Margin="0,16,0,16">
      <StackPanel>
        <TextBlock Text="Nhập tên người chơi:" Margin="0,0,0,8"/>
        <TextBox x:Name="PlayerNameBox" FontSize="18" Padding="8"/>
        <Button Content="Bắt đầu trò chơi" Margin="0,14,0,0" Click="Start_Click"/>
        <TextBlock x:Name="StatusText" Margin="0,12,0,0" Opacity="0.8"/>
      </StackPanel>
    </Border>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
        <TextBlock Text="💰 Xu:"/>
        <TextBlock x:Name="CoinText" FontWeight="Bold" Margin="4,0,0,0"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="Tổng tiền thưởng:"/>
        <TextBlock x:Name="PrizeText" FontWeight="Bold" Margin="4,0,0,0"/>
      </StackPanel>
    </StackPanel>
  </Grid>
</Window>
